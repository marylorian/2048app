import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  controls: {
    alignItems: "center",
    gap: 8,
    marginTop: 18
  },
  horizontalControls: {
    flexDirection: "row",
    gap: 8
  }
});
